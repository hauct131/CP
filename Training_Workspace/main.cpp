#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define int ll

void solve(){
    int n; cin >> n;
    cout << n << endl;
    return;
}

signed main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    if(fopen("input.txt", "r")){
        freopen("input.txt", "r", stdin); // Đọc từ input
        freopen("output.txt", "w", stdout); // Ghi ra output
    }
    solve();
    return 0;
}
